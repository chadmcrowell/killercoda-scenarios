
<br>

### Welcome!

A team reports that their application Pod is stuck in `Pending` state. Investigation reveals a storage configuration problem.

In this scenario, you'll **diagnose** the root cause of a stuck PVC and **fix** the misconfiguration so the Pod can reach `Running` state.

**ENJOY!**

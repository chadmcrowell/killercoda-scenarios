
<br>

### Welcome!

Several application pods are stuck in `Pending` even though `kubectl top nodes` shows plenty of free CPU and memory. One worker node has recently gone into `NotReady` and the workloads that were running there are no longer scheduled.

Your job is to **diagnose** why the scheduler won't place pods and **fix** the broken node so all replicas reach `Running`.

**ENJOY!**

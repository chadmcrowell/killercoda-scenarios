Using `kubectl`, verify the Gateway API CRDs are installed in your cluster.

<br>
<details><summary>Solution</summary>
<br>

```bash
# Verify the CRDs are installed
kubectl get crds | grep gateway

```{{exec}}

</details>
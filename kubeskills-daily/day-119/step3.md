## Step 3 — Apply the Fix

Update the deployment to use preferred pod anti-affinity instead of required, or adjust the topology key to a less restrictive domain, then confirm all replicas reach Running state and are distributed across nodes.

## Step 3 — Apply the Fix

Clean up the leaked IP allocations by removing the stale pods and verify that the available IP count increases, then review the node subnet size configuration and document what pod CIDR size would have prevented the exhaustion.

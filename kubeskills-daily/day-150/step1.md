## Step 1 — Investigate the Problem

Examine the pending pod events and identify the specific error message indicating an IP allocation failure, then check the node status to see how many pod IP addresses have been allocated versus the maximum allowed per node.

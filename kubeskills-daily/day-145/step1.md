## Step 1 — Investigate the Problem

From a pod in the first tenant namespace, attempt to reach services running in the second tenant namespace using their DNS names. Observe whether the connection succeeds. Then check whether any resource quotas or limit ranges are configured for either namespace.

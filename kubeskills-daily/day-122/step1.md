## Step 1 — Investigate the Problem

Deploy the provided workload that writes data to the container filesystem and observe that it gets evicted after a short time, then examine pod events and node events to identify the eviction reason and the resource that was exhausted.

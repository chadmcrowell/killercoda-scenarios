## Step 1 — Investigate the Problem

Locate the audit policy file referenced in the API server configuration and open it to review its contents. Identify any rules that use a None audit level and determine which resource types and operations are being silently dropped as a result.

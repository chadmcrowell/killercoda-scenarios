## Step 3 — Apply the Fix

Update the Pod Disruption Budget with values that allow the rolling update to make progress while still protecting against complete unavailability. Observe the rollout resuming and completing, then verify all pods are running the new version.

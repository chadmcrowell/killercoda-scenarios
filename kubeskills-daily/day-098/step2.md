## Step 2 — Identify the Root Cause

Calculate whether the current PDB configuration is mathematically compatible with the rolling update strategy defined in the deployment. Determine how many replicas must be available at all times according to the PDB and compare that to what the rolling update strategy allows.

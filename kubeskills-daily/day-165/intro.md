# Network Policy and DNS - Blocking Traffic to CoreDNS

A security team has applied a deny-all egress network policy to the payments namespace to meet a compliance requirement. Shortly after the policy was applied, the payment service started failing with DNS resolution errors. You need to fix the network policy without removing the security restriction.

> **Day 165 of KubeSkills Daily** — Fail Fast, Learn Faster

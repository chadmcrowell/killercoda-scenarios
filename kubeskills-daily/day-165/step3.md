## Step 3 — Apply the Fix

Apply the updated network policy to the namespace and verify from inside the application pod that DNS resolution now works and that the application can successfully connect to the backend service.

## Step 1 — Investigate the Problem

Inspect the existing network policy in the payments namespace, then exec into the failing application pod and attempt a DNS lookup to confirm that DNS resolution is the root cause of the application failure.

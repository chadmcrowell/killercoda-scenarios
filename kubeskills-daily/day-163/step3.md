## Step 3 — Apply the Fix

Either correct the readiness probe endpoint in the deployment specification and trigger a new rollout, or perform a rollout undo to return to the last known good revision, then confirm all pods reach the ready state.

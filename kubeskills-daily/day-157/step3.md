## Step 3 — Apply the Fix

Update each manifest to use the currently supported API version, adjusting any fields that have changed between versions. Apply all updated manifests to the cluster and verify each resource is created or updated successfully. Run the pluto detection tool or equivalent API scan against the entire manifests directory to confirm no deprecated API versions remain.

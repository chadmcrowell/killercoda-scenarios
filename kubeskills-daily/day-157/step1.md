## Step 1 — Investigate the Problem

Attempt to apply the provided manifest files against the upgraded cluster and observe the error messages returned for each one. Identify which API groups and versions are no longer available in the current Kubernetes version by examining the error output and cross-referencing with the available API resources on the cluster.

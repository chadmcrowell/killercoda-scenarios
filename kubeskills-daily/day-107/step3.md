## Step 3 — Apply the Fix

Update the priority class assignments so the production deployment uses a higher priority value than the batch job, then verify that under simulated resource pressure the production pods remain running while batch pods are the candidates for preemption.

## Step 3 — Apply the Fix

Execute the certificate renewal process using the appropriate cluster management tool, then restart the affected control plane component pods to ensure they load the newly issued certificates, and verify that all components return to a healthy state.

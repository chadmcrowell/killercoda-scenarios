<br>

### Day 19: ServiceAccount Tokens - Projection Problems

Tokens are not guaranteed—defaults, overrides, and projections change everything. You'll see default automounting, disable it per pod and ServiceAccount, override at the pod, create projected tokens with expiration and audiences, watch rotation, request tokens on demand, compare legacy secrets, and verify bound tokens.

**ENJOY!**

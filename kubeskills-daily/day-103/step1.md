## Step 1 — Investigate the Problem

Audit the versions of all components in the cluster. Check the API server version, then check the kubelet and kube-proxy version on every node. Identify which nodes are running versions that fall outside the supported skew policy relative to the control plane.

## Step 2 — Identify the Root Cause

Attempt to use a feature on a pod scheduled to an older node and observe the failure. Then schedule the same workload on a node running a compliant version and confirm it works correctly. This confirms that the version skew is causing the observed failures.

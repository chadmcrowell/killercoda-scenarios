## Step 3 — Apply the Fix

Walk through the restore process by restoring the snapshot to a new data directory and review the steps that would be required to point the etcd static pod manifest at the restored data directory to complete a full recovery.

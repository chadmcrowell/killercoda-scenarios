## Step 3 — Apply the Fix

Update the deployment to use corrected affinity rules that match the labels actually present on the cluster nodes. Confirm that pods transition from Pending to Running and verify the nodes they landed on match the intended placement strategy.

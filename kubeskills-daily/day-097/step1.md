## Step 1 — Investigate the Problem

Inspect the deployment and identify all pods that are stuck in Pending state. Then examine the scheduler events on those pods to understand what message the scheduler is reporting about why placement is failing.

## Step 1 — Investigate the Problem

Examine the failing pods and confirm that sidecar containers are running alongside the application containers, then review the proxy container logs to identify what traffic is being intercepted and whether any errors are being reported by the proxy.

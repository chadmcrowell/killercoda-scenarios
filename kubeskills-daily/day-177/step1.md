## Step 1 — Investigate the Problem

List the existing PriorityClass objects in the cluster and note their values. Then describe the pods that were recently evicted and identify the PriorityClass assigned to each one. Check the scheduler events to confirm preemption was the eviction cause.

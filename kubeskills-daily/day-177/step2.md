## Step 2 — Identify the Root Cause

Create three PriorityClass objects representing critical production traffic, standard application workloads, and batch processing jobs with appropriate priority values that clearly separate each tier. Update the batch job to use the batch processing priority class.

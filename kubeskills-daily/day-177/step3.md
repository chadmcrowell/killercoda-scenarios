## Step 3 — Apply the Fix

Deploy representative pods for each priority tier and fill the cluster to capacity. Then deploy an additional critical-priority pod and observe the event stream to confirm that only batch-tier pods are preempted and that the critical pod is scheduled successfully.

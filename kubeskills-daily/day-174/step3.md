## Step 3 — Apply the Fix

Run a defragmentation operation against each etcd member to reclaim the physical disk space freed by compaction. After defragmentation, check the endpoint status again to confirm the database size has decreased.

## Step 1 — Investigate the Problem

Use kubeadm certificate checking commands to list all certificates, their expiration dates, and the certificate authority that signed them. Identify which certificates expire soonest and which components rely on them.

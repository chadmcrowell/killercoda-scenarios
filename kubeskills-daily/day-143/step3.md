## Step 3 — Apply the Fix

Verify that the API server, controller manager, and scheduler are all running again after the component restart. Run a certificate check again to confirm all expiry dates have been pushed forward. Confirm you can still run cluster commands successfully.

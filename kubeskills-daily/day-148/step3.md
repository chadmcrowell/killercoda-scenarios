## Step 3 — Apply the Fix

Update each deployment to use a specific version tag and set the image pull policy to Always, then document how you would extend this to use image digests for the strongest possible immutability guarantee.

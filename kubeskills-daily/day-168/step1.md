## Step 1 — Investigate the Problem

Create two PriorityClass objects, one for production workloads with a high priority value and one for batch workloads with a low priority value. Deploy enough low-priority batch pods to consume nearly all available node resources.

## Step 3 — Apply the Fix

Update the deployment with a memory limit that accommodates peak usage plus a safety buffer, and also set a memory request that reflects the expected baseline consumption, then monitor the pod to confirm it runs without being killed.

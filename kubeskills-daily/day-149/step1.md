## Step 1 — Investigate the Problem

Examine the running workloads in the target namespace and use the network policy list to confirm that no egress restrictions are currently applied, then reason about what external destinations each application legitimately needs to contact.

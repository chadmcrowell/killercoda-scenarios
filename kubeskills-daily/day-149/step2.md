## Step 2 — Identify the Root Cause

Apply a default deny egress policy to the namespace and immediately verify that DNS resolution is broken for pods in the namespace, then understand why DNS egress must be explicitly permitted.

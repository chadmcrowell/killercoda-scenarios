## Step 2: Create basic Helm chart

```bash
mkdir -p /tmp/mycharts
cd /tmp/mycharts
helm create broken-app

# Check chart structure
ls -la broken-app/
```{{exec}}

Generate a starter chart to break and fix.

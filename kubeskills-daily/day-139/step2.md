## Step 2 — Identify the Root Cause

Examine the nodes in the cluster by listing them with their labels shown. Compare the labels present on each node against the NodeSelector values defined in the pod specification. Identify which label requirement cannot be satisfied by any node.

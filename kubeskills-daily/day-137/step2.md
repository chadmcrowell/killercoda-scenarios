## Step 2 — Identify the Root Cause

Examine the readiness probe configuration on the deployment and identify the HTTP path and port being checked. Then inspect the running application to determine what paths are actually available and why the configured probe path returns a failing status code.

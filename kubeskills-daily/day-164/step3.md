## Step 3 — Apply the Fix

If the filesystem has not expanded, delete the pod to allow the kubelet to restart it with the updated volume, then verify the new disk size is visible inside the rescheduled pod.

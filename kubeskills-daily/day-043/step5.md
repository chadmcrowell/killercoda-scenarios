## Step 5: Test sampling rate (drops spans)

```bash
# Example sampling env vars (conceptual):
# OTEL_TRACES_SAMPLER=parentbased_traceidratio
# OTEL_TRACES_SAMPLER_ARG=0.1
```

Lower sampling ratios reduce captured traces.

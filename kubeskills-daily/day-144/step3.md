## Step 3 — Apply the Fix

Perform a series of test operations including listing pods, reading a secret, updating a configmap, and deleting a test secret. Then open the audit log file and filter entries to find each of these operations. Confirm the user, timestamp, and outcome are all captured.

## Step 3 — Apply the Fix

Create new narrowly scoped roles that grant only the specific verbs and resource types each workload requires, then update the role bindings to reference the new roles and remove any unnecessary cluster role bindings.

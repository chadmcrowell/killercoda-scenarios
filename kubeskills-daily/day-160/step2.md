## Step 2 — Identify the Root Cause

Use the etcdctl tool to take a snapshot backup of the live etcd database, specifying the correct endpoint and TLS certificate paths, and save the snapshot to a local file path on the control plane node.

## Step 2 — Identify the Root Cause

Review the warning messages to identify which workloads are running as root or using privileged settings, then update those workload specifications to remove the violations by setting security contexts appropriately.

## Step 3 — Apply the Fix

Switch the namespace label from warn mode to enforce mode and attempt to deploy a non-compliant pod to confirm it is rejected, then verify all legitimate workloads still deploy successfully.

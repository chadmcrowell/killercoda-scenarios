## Step 3 — Apply the Fix

Resolve the scheduling failure either by updating the deployment pod template to use a node selector that matches existing node labels, or by adding the required label to the appropriate node, then confirm pods begin scheduling.

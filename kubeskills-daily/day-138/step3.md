## Step 3 — Apply the Fix

Drain the node using the appropriate flags for DaemonSet pods and confirm that all evicted pods have been successfully rescheduled onto the remaining nodes. After simulating the maintenance work, uncordon the node and verify that it returns to an active scheduling state.

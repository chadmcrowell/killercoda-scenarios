## Step 1 — Investigate the Problem

Describe the affected node and review its conditions section to confirm DiskPressure is active. Look at the node events to see which pods were evicted and why. Then identify the runaway workload by examining pod logs and checking which containers are consuming the most ephemeral storage.

## Step 3 — Apply the Fix

Apply the corrected CNI configuration that fixes the cross-node routing issue. Restart the affected CNI components and verify that connectivity tests between pods on different nodes now succeed consistently.

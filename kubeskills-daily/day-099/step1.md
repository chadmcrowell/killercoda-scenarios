## Step 1 — Investigate the Problem

Deploy a simple client pod and test connectivity to a backend pod on the same node, then test connectivity to a backend pod on a different node. Document which connections succeed and which fail to establish the scope of the networking problem.

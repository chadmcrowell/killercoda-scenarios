## Step 1 — Investigate the Problem

Attempt to drain a target node and observe the behavior, then list all PodDisruptionBudgets across all namespaces and examine the min available and max unavailable settings relative to the current replica counts of the protected deployments.

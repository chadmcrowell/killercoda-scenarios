## Step 2 — Identify the Root Cause

Update the ConfigMap with a clearly identifiable new value, then observe the behavior of running pods over time by checking both the environment variables inside the container and any mounted configuration files to see which method updates and which does not.

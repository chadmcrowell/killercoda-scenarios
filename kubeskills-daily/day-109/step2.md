## Step 2 — Identify the Root Cause

Inspect the image pull secret if it exists by describing it and checking that its type is correct and that the dockerconfigjson data references the right registry hostname and contains non-expired credentials.
